/**
 * List of props that may be updated without re-initializing the player.
 *
 * @type {string[]}
 */
const updatableProps = [
  'aspectRatio',
  'isMuted',
  'isAutoPlay',
];

export default updatableProps;
